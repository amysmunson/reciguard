# Recipes

## Running on iOS
npx expo start --tunnel

## Plan
- Shared? Recipe Book
- (Must be able to private some recipes later, maybe public and private tags/folder)
- Maybe group allergy option: select your group of people, tells you which recipes are safe
    - Should always mark your own allergy
- Recipes with allergies aren't inherently uneditable, so let them still see, mark specific ingredients as the problem


Note: need edit recipe screen to allow scroll when keyboard up
More space around back buttons so I can miss and still go back
Highlight bottom bar based on where you are?



 WARN  Looks like you're passing an inline function for 'component' prop for the screen 'Loading' (e.g. component={() => <SomeComponent />}). Passing an inline function will cause the component state to be lost on re-render and cause perf issues since it's re-created every render. You can pass the function as children to 'Screen' instead to achieve the desired behaviour.